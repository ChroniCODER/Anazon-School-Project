{% block navBarSearch %}
	<nav class="navbar navbar-expand-lg navbar-light bg-dark">
		<div class="container-fluid">
			<a class="navbar-brand text-white" href="#">ANAZON</a>
			<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>

			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<form class="d-flex">
					<input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
					<button class="btn btn-outline-success" type="submit">Search</button>
				</form>
				
				<ul class="navbar-nav ml-auto mb-2 mb-lg-0">
					
					<li class="nav-item" data-controller="cartheader">
						
						<i class="fa-solid fa-house text-white"></i><a href="{{ path('app_categories') }}">Accueil</a>
						{% if app.user %}
							<i class="fa-solid fa-right-from-bracket text-white"></i> <a href="{{ path('app_logout') }}">Déconnexion</a>
						{% else %}
							<i class="fa-solid fa-user text-white"></i> <a href="{{ path('app_login') }}">Connexion</a>
						{% endif %}
						<i class="fa-solid fa-cart-shopping text-white"></i>
						<a href="{{ path('app_cart') }}">
						Mon panier (<span data-cartheader-target="quantity"> 
							{% if (app.request.session.has('cart')) %}
								{{ (app.request.session.get('cart').countTotalProducts() ) }}
							{% else %}
								0 
							{% endif %}
							</span>)</a>

					</li>
					
				</ul>

			</div>
		</div>
	</nav>
{% endblock %}
